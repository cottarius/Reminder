spring:
  application:
    name: reminder

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/reminder_db
    username: postgres
    password: postgres